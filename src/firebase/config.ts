/**
 * This file contains the Firebase configuration object.
 *
 * It is used by the `initializeFirebase` function in `src/firebase/index.ts`
 * to initialize the Firebase app.
 *
 * This file is public and can be checked into version control. It is not a
 * secret and does not contain any sensitive information.
 */
export const firebaseConfig = {
  apiKey: "API_KEY",
  authDomain: "PROJECT_ID.firebaseapp.com",
  projectId: "PROJECT_ID",
  storageBucket: "PROJECT_ID.appspot.com",
  messagingSenderId: "SENDER_ID",
  appId: "APP_ID",
};
